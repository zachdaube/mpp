name: "gnn_small"

model:
  type: "GNN"
  hidden_dim: 64
  num_layers: 2
  dropout: 0.2

data:
  featurizer: "graph"

training:
  lr: 0.001
  batch_size: 32
  max_epochs: 100
  patience: 20
  seed: 42