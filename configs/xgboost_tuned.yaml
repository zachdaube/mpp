name: "xgboost_tuned"

model:
  type: "XGBoost"
  n_estimators: 500
  max_depth: 8
  learning_rate: 0.05
  subsample: 0.8
  colsample_bytree: 0.8
  gamma: 0.1
  reg_alpha: 0.1
  reg_lambda: 1.0

data:
  featurizer: "combined"
  fp_size: 2048
  radius: 3

training:
  seed: 42