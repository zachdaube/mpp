name: "gat_large"

model:
  type: "GAT"
  hidden_dim: 256
  num_layers: 4
  dropout: 0.3
  heads: 4

data:
  featurizer: "graph"

training:
  lr: 0.001
  batch_size: 32
  max_epochs: 100
  patience: 20
  seed: 42